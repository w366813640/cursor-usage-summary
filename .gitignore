node_modules
.pnpm-store
.turbo
dist
build
.vite
out
*.log
.DS_Store
.env
.env.local
.env.*.local
coverage
storybook-static

# 用户的 Cursor 用量数据，不入版本控制
input/*.csv
!input/.gitkeep

# 本地 E2E 验收截图 / 缓存日志 — 太大且会变化
_temp/

# Trellis / Cursor / Codex 本地编辑器配置
# Per-developer editor + assistant settings — never shared, not load-bearing.
.cursor/rules/
.cursor/settings.json
# .codex/
# .agents/
